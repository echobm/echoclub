<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.question.infra.basic.mapper.QuestionCategoryDao">

    <resultMap type="com.echo.question.infra.basic.entity.QuestionCategory" id="QuestionCategoryMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="categoryName" column="category_name" jdbcType="VARCHAR"/>
        <result property="categoryType" column="category_type" jdbcType="INTEGER"/>
        <result property="categorImg" column="categor_img" jdbcType="VARCHAR"/>
        <result property="parentId" column="parent_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="QuestionCategoryMap">
        select
id, category_name, category_type, categor_img, parent_id
        from question_category
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="QuestionCategoryMap">
        select
id, category_name, category_type, categor_img, parent_id
        from question_category
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="categoryName != null and categoryName != ''">
                and category_name = #{categoryName}
            </if>
            <if test="categoryType != null">
                and category_type = #{categoryType}
            </if>
            <if test="categorImg != null and categorImg != ''">
                and categor_img = #{categorImg}
            </if>
            <if test="parentId != null">
                and parent_id = #{parentId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from question_category
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="categoryName != null and categoryName != ''">
                and category_name = #{categoryName}
            </if>
            <if test="categoryType != null">
                and category_type = #{categoryType}
            </if>
            <if test="categorImg != null and categorImg != ''">
                and categor_img = #{categorImg}
            </if>
            <if test="parentId != null">
                and parent_id = #{parentId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into question_category(category_name, category_type, categor_img, parent_id)
        values (#{categoryName}, #{categoryType}, #{categorImg}, #{parentId})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into question_category(category_name, category_type, categor_img, parent_id)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.categoryName}, #{entity.categoryType}, #{entity.categorImg}, #{entity.parentId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into question_category(category_name, category_type, categor_img, parent_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.categoryName}, #{entity.categoryType}, #{entity.categorImg}, #{entity.parentId})
        </foreach>
        on duplicate key update
category_name = values(category_name),
category_type = values(category_type),
categor_img = values(categor_img),
parent_id = values(parent_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update question_category
        <set>
            <if test="categoryName != null and categoryName != ''">
                category_name = #{categoryName},
            </if>
            <if test="categoryType != null">
                category_type = #{categoryType},
            </if>
            <if test="categorImg != null and categorImg != ''">
                categor_img = #{categorImg},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from question_category where id = #{id}
    </delete>

</mapper>

